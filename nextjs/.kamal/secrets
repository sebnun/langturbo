# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# Option 1: Read secrets from the environment
# KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Option 2: Read secrets via a command
# RAILS_MASTER_KEY=$(cat config/master.key)
KAMAL_REGISTRY_PASSWORD=$(cat .env | grep KAMAL_REGISTRY_PASSWORD | cut -d'=' -f2- | tr -d '"')
POSTGRES_PASSWORD=$(cat .env | grep POSTGRES_PASSWORD | cut -d'=' -f2- | tr -d '"')
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=$(cat .env | grep NEXT_SERVER_ACTIONS_ENCRYPTION_KEY | cut -d'=' -f2- | tr -d '"')

GOOGLE_CLOUD_PROJECT=$(cat .env | grep GOOGLE_CLOUD_PROJECT | cut -d'=' -f2- | tr -d '"')
GOOGLE_KEY_FILE=$(cat .env | grep GOOGLE_KEY_FILE | cut -d'=' -f2- | tr -d '"')
GOOGLE_STORAGE_KEY_FILE=$(cat .env | grep GOOGLE_STORAGE_KEY_FILE | cut -d'=' -f2- | tr -d '"')

BETTER_AUTH_SECRET=$(cat .env | grep BETTER_AUTH_SECRET | cut -d'=' -f2- | tr -d '"')

USER_AGENT=$(cat .env | grep USER_AGENT | cut -d'=' -f2- | tr -d '"')

SENTRY_AUTH_TOKEN=$(cat .env | grep SENTRY_AUTH_TOKEN | cut -d'=' -f2- | tr -d '"')

AWS_ACCESS_KEY_ID=$(cat .env | grep AWS_ACCESS_KEY_ID | cut -d'=' -f2- | tr -d '"')
AWS_SECRET_ACCESS_KEY=$(cat .env | grep AWS_SECRET_ACCESS_KEY | cut -d'=' -f2- | tr -d '"')

LIVEKIT_API_SECRET=$(cat .env | grep LIVEKIT_API_SECRET | cut -d'=' -f2- | tr -d '"')
LIVEKIT_API_KEY=$(cat .env | grep LIVEKIT_API_KEY | cut -d'=' -f2- | tr -d '"')

# Option 3: Read secrets via kamal secrets helpers
# These will handle logging in and fetching the secrets in as few calls as possible
# There are adapters for 1Password, LastPass + Bitwarden
#
# SECRETS=$(kamal secrets fetch --adapter 1password --account my-account --from MyVault/MyItem KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY)
# KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD $SECRETS)
# RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY $SECRETS)
